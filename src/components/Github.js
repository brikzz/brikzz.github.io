import React from 'react';
import image from './githublogo.jpg';

const Github = () => {
    return (
        <div>
            <img src = {image} width = {30} height = {30} alt = "Github" />
        </div>
    );
};

export default Github;