import React from 'react';
import image from './instalogo.jpg';

const Instagram = () => {
    return (
        <div>
            <img src = {image} width = {30} height = {30} alt = "Me" />
        </div>
    );
};

export default Instagram;